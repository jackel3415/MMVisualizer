# MMVisualizer

A browser-based preview tool for [MagicMirror²](https://magicmirror.builders/) configs. Load your `config.js` and see your mirror layout instantly — no Raspberry Pi needed.

## Usage

1. Open `index.html` in any browser
2. Load your config one of three ways:
   - **Drag & drop** `config.js` onto the page
   - Click **Load config.js** to browse for the file
   - Use the **Paste** tab in the sidebar to paste config text directly
3. Click any module card to inspect its full configuration

## Features

- Accurate MagicMirror region layout (all 13 positions)
- Module detail panel showing full config properties
- Multiple resolution presets (1920×1080, portrait, RPi 7", etc.)
- Scale slider for fitting large resolutions on screen
- Sidebar module list with position indicators
- Disabled module toggling
- Custom module highlighting
- Works entirely offline — single HTML file, no dependencies

## No install required

Everything runs in the browser. Just open `index.html`.
